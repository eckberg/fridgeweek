{
  "name": "fridgeweek-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Printable weekly sheet for the fridge, filled in by hand.",
  "license": "MIT",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=26"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "26.7.0",
      "onFail": "error"
    }
  },
  "scripts": {
    "dev": "pnpm --filter @fridgeweek/web dev",
    "build": "pnpm -r build",
    "deploy:cloudflare": "pnpm --filter @fridgeweek/web run deploy:cloudflare",
    "deploy:dry-run": "pnpm --filter @fridgeweek/web run deploy:dry-run",
    "test": "pnpm -r test",
    "test:visual": "pnpm -r test:visual",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "pnpm -r typecheck",
    "sample": "pnpm --filter @fridgeweek/core sample",
    "test:e2e": "pnpm -r test:e2e"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "typescript": "^7.0.2"
  }
}
